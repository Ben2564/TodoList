{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp64/www/Claverie/symfony/myapp/src/Controller/PostController.php'|file_link(0) }}">src/Controller/PostController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp64/www/Claverie/symfony/myapp/templates/post/index.html.twig'|file_link(0) }}">templates/post/index.html.twig</a></code></li>
    </ul>
</div>
<div>
{{ dump(posts) }}
</div>
<div>
    {% for post in posts %}
    <div>
        <p>{{post.id}}</p>
        <p>{{post.title}}</p>
        <p>{{post.body}}</p>
        <p>{{post.userId}}</p>
        {% for tag in post.tags %}
        <ul>
            <li>{{ tag }}</li>
        </ul>
        {% endfor %}
        <p>{{post.reactions}}</p>
    </div>
    {% endfor %}
</div>
{% endblock %}
